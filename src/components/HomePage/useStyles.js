import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  mainContainer: {
    textAlign: 'center',
  },
  nav: {
    marginBottom: '3em',
    cursor: 'default',
  },
}));

export default useStyles;
